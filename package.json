{
  "name": "glob-rewriter",
  "version": "1.0.0",
  "description": "Rewrites paths, using two globs as patterns",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/tape test/tests.js"
  },
  "license": "MIT",
  "devDependencies": {
    "tape": "^4.6.3"
  },
  "dependencies": {
    "glob-to-regexp": "git://github.com/roccivic/glob-to-regexp.git#capture-groups"
  }
}
